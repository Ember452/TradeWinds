# AGENTS.md — AI 开发指南

本文件是 AI（以及新加入的人类协作者）在本仓库开发时的行为准则与工程规范。项目背景见 [docs/design.md](docs/design.md)。

**权衡说明**：以下准则偏向"谨慎优先于速度"。琐碎任务可自行判断，放宽执行。

## 0. 文档阅读地图

开始任务前，按任务类型先读对应文档，不要凭猜测开发：

| 你要做的任务 | 必读 | 参考 |
|---|---|---|
| 任何开发任务 | 本文件全文 | — |
| 涉及产品范围、功能取舍、用户流程 | docs/design.md 第 1-3 节 | docs/scaling-plan.md |
| 涉及 Agent 编排、角色、prompt、工具 | docs/design.md 第 4 节 | docs/design.md 第 9 节（测试策略） |
| 涉及数据模型、API 接口 | docs/design.md 第 5-6 节 | — |
| 涉及部署、基础设施、技术选型 | docs/design.md 第 8 节 | docs/scaling-plan.md |
| 涉及规模演进、性能、成本治理 | docs/scaling-plan.md | — |
| 写代码前的实现计划 | docs/plans/ 下对应计划文档（如有） | — |

规则：如果实现中发现你的做法与设计文档冲突，**停下来说明冲突**，经确认后先改文档再改代码。文档与代码不一致时，以提交时间新者为准并立即修复另一者。

## 1. 先想后写

**不要臆测。不要隐藏困惑。把取舍摆到台面上。**

实现之前：

- 显式陈述你的假设。不确定就问。
- 存在多种理解时，全部列出——不要默默替人选一个。
- 存在更简单的做法时，直说。该反驳时反驳。
- 有不清楚的地方就停下，指出来，提问。

## 2. 简单优先

**解决问题的最少代码。不做任何投机性设计。**

- 不做超出要求的功能。
- 单次使用的代码不做抽象。
- 不做没被要求的"灵活性"或"可配置性"。
- 不为不可能发生的场景写错误处理。
- 如果 200 行能写成 50 行，重写。

自问："资深工程师会觉得这过度设计吗？"会，就简化。

## 3. 外科手术式修改

**只动必须动的。只清理自己制造的垃圾。**

修改现有代码时：

- 不"顺手改进"无关代码、注释或格式。
- 不重构没坏的东西。
- 匹配现有风格，即使你有不同偏好。
- 发现无关的死代码：提及，不删除。

你的改动产生了孤儿代码（不再使用的 import/变量/函数）时：

- 删除**你的改动**导致失效的部分。
- 不删改动前就存在的死代码，除非被要求。

检验标准：**每一行变更都能追溯到当前任务。**

## 4. 目标驱动执行

**先定义成功标准，循环直到验证通过。**

把任务转成可验证的目标：

- "加个校验" → "为非法输入写测试，然后让测试通过"
- "修个 bug" → "写一个能复现 bug 的测试，然后让它通过"
- "重构 X" → "确保重构前后测试都通过"

多步任务先给简短计划：

```
1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
```

强成功标准让你能独立循环；弱标准（"把它弄好"）只会导致反复澄清。

## 5. 文件架构规范

目标结构（实现期可微调，但层级意图不变）：

```
TradeWinds/
├── AGENTS.md                  # 本文件
├── README.md                  # 项目门面
├── pyproject.toml             # 依赖与工具配置（ruff/mypy/pytest 统一在此）
├── docs/
│   ├── design.md              # 设计文档
│   ├── scaling-plan.md        # 扩展计划
│   └── plans/                 # 实现计划文档（写代码前产出）
├── src/tradewinds/
│   ├── api/                   # FastAPI 路由层：请求/响应模型、依赖注入，不含业务逻辑
│   ├── agents/                # 编排层 + 四角色；prompt 单独文件，不内嵌字符串
│   ├── tools/                 # 信息源客户端（arxiv/hn/github/web），每个源一个模块
│   ├── services/              # 业务逻辑层：路由薄、服务厚
│   ├── tasks/                 # Celery 任务定义与调度逻辑
│   ├── models/                # 数据库模型与迁移
│   ├── core/                  # 配置、日志、安全、异常定义
│   └── push/                  # 邮件与通知渠道
├── frontend/                  # React SPA（Vite + TypeScript）
└── tests/                     # 与 src 结构镜像
```

约束：

- **依赖方向单向**：api → services → models/agents/tools；下层不得反向 import 上层；core 不依赖任何业务模块
- 路由层只做参数校验与编排调用，业务逻辑在 services
- prompt、金标集等 Agent 资产放独立文件/目录，与代码分离，便于评审与评测
- 配置一律从环境变量/配置模块读取，禁止硬编码密钥、URL、阈值之外的魔数

## 6. 文件拆分规范

**不看文件行数拆分**。行数是结果不是原因。按以下信号判断是否拆分：

1. **职责多于一**：一个文件需要用"和"才能描述完它做什么（如"处理认证和限流"），拆
2. **变化原因多于一**：改 A 功能会碰到 B 功能的代码，说明它们住在同一文件里，拆
3. **上下文可理解性**：理解一个单元需要同时滚动着看太多无关内容，拆
4. **可独立测试性**：一段逻辑值得单独测试却无法脱离所在文件单独 import，拆
5. **接口清晰度**：抽出的单元能用一句话说清"输入什么、输出什么、依赖什么"，才值得拆出——说不清就先不拆

反规则：

- 不为"目录好看"而拆出只有一个调用者的单函数文件
- 不建 `utils.py`/`helpers.py` 杂物间——每个模块必须有明确主题命名
- 拆分后的公共接口（被跨模块调用的函数/类）必须有类型标注和 docstring

## 7. 代码质量规范

- **类型标注全量**：所有函数签名有类型标注，`mypy` 无错误为准；Pydantic 模型贯穿配置、Agent 角色间数据、API 请求响应
- **测试与实现同步**：新功能先写测试（TDD）；所有 LLM 调用在测试中必须 mock；测试不依赖网络与外部服务
- **异步规范**：IO 操作（DB/HTTP/Redis）一律 async，禁止在 async 路径中调用阻塞函数；CPU 密集或同步库调用丢进任务队列或线程池
- **错误处理**：业务错误用显式异常类型并集中定义；禁止裸 `except:`；Agent 工具失败作为结果回注模型而非中断（见 design.md 4.1）；重试一律带指数退避与上限
- **日志**：统一 structlog 结构化日志，关键路径（任务执行、LLM 调用、推送）必须有关键字段的 INFO 日志；日志不输出敏感信息（密钥、密码、完整用户内容）
- **命名**：模块名、函数名可读出意图；缩写除通用术语（url、api）外不使用
- **依赖纪律**：新增第三方依赖需说明理由并确认维护活跃度；能用标准库/已有依赖解决的不新增

## 8. Git 与提交规范

**核心规则：不主动提交。** 只有用户明确要求时才 commit；用户要求"提交"时只提交当前任务相关文件。

提交信息遵循 Conventional Commits：

```
<type>(<scope>): <描述>

[可选正文：动机与影响]
```

- type：`feat` / `fix` / `docs` / `refactor` / `test` / `chore` / `perf`
- scope：模块名（`agents`、`api`、`tasks`、`docs` 等）
- 描述用中文、祈使语气、结尾不加句号，50 字内；正文解释"为什么"而不是"改了什么"
- 一次提交只做一件事；混合了重构与功能的改动拆开提交
- 禁止 `git commit -m "update"` 之类的无效信息；禁止提交 `.env`、密钥、构建产物

示例：

```
feat(agents): 实现编排层工具循环与结构化输出重试
fix(push): 抑制规则未按聚类去重导致重复推送
docs(design): 数据模型一节移除字段级约束
```

## 9. 文档同步纪律

- 实现与设计冲突 → 先停下沟通，确认后**先改文档再改代码**，两者同一批提交
- 设计文档中的"暂定"值（推送阈值、循环上限等）在实现定稿后回写文档并去掉"暂定"
- 每个实现计划完成后，检查 docs/plans/ 中的计划是否与实际交付一致，不一致处修正

---

**这些准则生效的标志**：diff 中没有多余改动；没有因过度设计导致的返工；澄清性问题发生在实现之前而不是犯错之后；文档与代码始终一致。
